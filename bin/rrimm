#!/bin/env ruby

begin
  load 'lib/rrimm.rb'
rescue LoadError
  require 'rrimm'
end

conf = RRImm::Config.new
conf.load(File.join(ENV['HOME'], '.config', 'rrimm', 'rrimm.rb'))

fetcher = RRImm::Fetcher.new(conf)
fetcher.fetch
